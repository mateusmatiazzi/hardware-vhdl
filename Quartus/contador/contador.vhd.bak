entity contador is
port(
	clk: in bit;
	ctrl: out bit_vector(3 downto 0)
);
end entity;
architecture algoritmico of contador is
begin
	process(clk, ctrl) is
	begin
		if clk'event and clk = '1' then
		
	end process;
end architecture;